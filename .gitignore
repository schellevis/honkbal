# Build output (deploy artifact, generated by CI — not committed; SPEC §8)
docs/
docs_v2/
version.txt

# Data cache (fetch results, last-known-good; CI uses actions/cache@v4)
.data/

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node (frontend tests: node:test + Playwright)
node_modules/
test-results/
playwright-report/
.playwright/

# OS / editor
.DS_Store
*.log
.env
.claude/settings.local.json
